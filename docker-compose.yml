services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: aiagent
      POSTGRES_PASSWORD: aiagent
      POSTGRES_DB: ai_agent_journey
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
